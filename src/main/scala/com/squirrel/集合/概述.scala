package com.squirrel.集合

import com.sun.prism.paint.Color

import scala.collection.{LinearSeq, SortedSet, mutable}

class 概述 {
  Traversable(1, 2, 3)
  Iterable("x", "y", "z")
  Map("x" -> 24, "y" -> 25, "z" -> 26)
  Set(Color.RED, Color.GREEN, Color.BLACK)
  SortedSet("hello", "world")
//  mutable.Buffer(x, y, z)
  IndexedSeq(1.0, 2.0)
//  LinearSeq(a, b, c)
}
